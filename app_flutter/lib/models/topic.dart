import 'package:flutter/material.dart';

class Topic {
  final String id;
  final String title;
  final IconData icon;
  final Color color;
  final int totalTip;

  Topic(this.id, this.title, this.icon, this.color, this.totalTip);
}