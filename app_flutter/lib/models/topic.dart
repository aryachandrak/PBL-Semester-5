import 'package:flutter/material.dart';

class Topic {
  final IconData icon;
  final Color color;
  final String title;
  final int totalTip;

  Topic(this.icon, this.color, this.title, this.totalTip);
}